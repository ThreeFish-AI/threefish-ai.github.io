### 2.3.4. 误差逆传播

从上文我们可以知道，神经网络学习的过程主要在于求损失函数关于权重参数的梯度。

我们演示了使用`数值微分`方式求梯度的过程，数值微分的方式求梯度简单、易于理解与实现，但有没有更高效的方式呢？

终于轮到`误差逆传播`算法出场了！

误差逆传播算法（`error BackPropagation`，简称 `BP 算法`）是基于数学推导的`解析性`梯度计算方法（`符号微分`，`Symbolic Differentiation`），按照求导的链式法则，局部导数会按正向传播的反方向传递。

#### 1. 链式法则

如果某个函数由复合函数表示，则该复合函数的导数可以用构成该复合函数的各个函数的导数的乘积表示。

如式 2.3.4.1 所示：

$$
    z = t^2 \\
    t = x + y                       \tag{2.3.4.1}
$$

z 关于 x 的导数这样求得：

$$
    \frac{\partial{z}}{\partial{x}} = \frac{\partial{z}}{\partial{t}} \frac{\partial{t}}{\partial{x}} = 2t \times 1 = 2(x + y)
                                    \tag{2.3.4.2}
$$

图 2.3.4.1 是在求 $\frac{\partial{z}}{\partial{x}}$ 的过程：

<!-- <div align=center>
    <img src='../images/ann/2.3.4-chain-rule.png'><br/>
    图 2.3.4.1 局部导数沿着函数正向计算的链路逆向传播
</div><br/> -->

参考 $\frac{\partial{z}}{\partial{x}}$ 的求导过程，可见一个复杂函数的求导问题可以分解组成该复杂函数的局部函数求导问题，沿着函数正向计算的链路，从最末端逆向计算每个局部函数的导数，最终相乘即是该复杂函数的导数。这就是 BP 算法的图解过程。

理解了 BP 算法，接下来我们用 BP 算法解释加法、乘法等常用计算在逆传播过程中的计算逻辑。

1. 加法的逆传播

以 $z = x + y$ 为例，其梯度 ($\frac{\partial{z}}{\partial{x}}$，$\frac{\partial{z}}{\partial{y}}$) 永远为 (1, 1)。

因此加法运算在逆传播时，总是将下游梯度乘以 1，即原封不动传递给上游。

我们用 `AddLayer` 类实现加法运算的前向传播与逆传播，代码实现参加 [Notebook](https://github.com/AfterShip/all-staff-writing-plan.deep-learning-basic/blob/master/runtime/Deep%20Learning.ipynb) 1.3.3 - 1。

1. 乘法的逆传播

以 $z = xy$ 为例，其梯度 ($\frac{\partial{z}}{\partial{x}}$，$\frac{\partial{z}}{\partial{y}}$) = (y, x)。

因此乘法运算的逆传播时，总是将下游梯度乘以上游相乘参数的值（翻转值）。比如 x 与 y 相乘，求关于 x 的偏导数时，y 是 x 的翻转值；求关于 y 的偏导数时，x 是 y 的翻转值。

我们用 `MulLayer` 类实现乘法运算的前向传播与逆传播，代码实现参加 [Notebook](https://github.com/AfterShip/all-staff-writing-plan.deep-learning-basic/blob/master/runtime/Deep%20Learning.ipynb) 1.3.3 - 2。

#### 2. 逆传播求梯度

以 $y = x_1x_2 + x_3$ 为例，求 $(x_1, x_2, x_3)$= (100, 2, 300) 时的梯度。

<!-- <div align=center>
    <img src='../images/ann/2.3.4-bp-gradient-demo.png'><br/>
    图 2.3.4.2 逆传播求梯度的计算链路，红色箭头表示逆传播的计算方向
</div><br/> -->

式 $y = x_1x_2 + x_3$ 的计算链路如图 2.3.4.2，我们通过上文的 MulLayer 和 AddLayer 可以直接进行前向传播求 y，以及逆传播求关于 $(x_1, x_2, x_3)$ 的梯度。

```python
x1, x2, x3 = 100, 2, 300
mul_layer = MulLayer()
add_layer = AddLayer()

# forward
a = mul_layer.forward(x1, x2)
y = add_layer.forward(a, x3)
print(y)                # 500

# backward
da, dx3 = add_layer.backward(1)
dx1, dx2 = mul_layer.backward(da)
print(dx1, dx2, dx3)    # (x2, x1, 1) = (2, 100, 1)
```

运行结果与计算图中所示 $(x_2, x_1, 1)$ 一致，可见逆传播求梯度的结果符合预期。

将以上逆传播求梯度的过程套用在神经网络损失函数关于权重参数的梯度求解上，我们就实现一个高效的神经网络学习算法，这就是 BP 算法。

```text
BP 算法实质是 LMS（Least Mean Square）算法的推广。LMS 试图使网络的输出均方误差最小化，用于神经元激活函数可微的感知机学习，LMS 推广到由非线性可微神经元组成的多层前馈网络，就是 BP 算法。

BP 算法是迄今最成功的神经网络学习算法，通常神经网络（不限于前馈神经网络）都使用 BP 算法进行训练。“BP 网络”特指使用 BP 算法训练的多层前馈神经网络。
```

---

在神经网络中，交叉熵损失（Cross-Entropy Loss）常用于分类任务，而通过误差逆传播（Backpropagation）计算其梯度是模型训练的关键过程。下面是如何计算交叉熵误差的梯度的详细说明。

### 交叉熵损失

交叉熵损失的公式为：

$$
    L(y, t) = -\sum_{i} t_i \log(y_i)
$$

其中：

- $y$ 是模型的输出（预测概率，通常是通过 softmax 函数获得）。
- $t$ 是真实标签（通常是 one-hot 编码）。

### 梯度计算

首先，我们用 $y$ 表示模型的输出，$t$ 表示真实标签。

交叉熵损失关于输出 $y$ 的梯度为：

$$
    \frac{\partial L}{\partial y_i} = -\frac{t_i}{y_i}
$$

对于所有 $i$，这可以以向量的形式表示为：

$$
    \frac{\partial L}{\partial y} = -\frac{t}{y}
$$

### 误差逆传播

假设神经网络的最后一层输出是经过 softmax 函数的 $y$，我们可以定义 softmax 函数如下：

$$
    y_j = \frac{e^{z_j}}{\sum_{k} e^{z_k}}
$$

其中 $z$ 是神经网络最后一层的未归一化的输出。

在误差逆传播中，我们需要根据损失函数 $L$ 的输出偏导数来更新权重。

#### 1. 计算关于 z 的梯度

使用链式法则，可以从 $L$ 到 $z$ 计算梯度：

$$
    \frac{\partial L}{\partial z_j} = \frac{\partial L}{\partial y_j} \cdot \frac{\partial y_j}{\partial z_j}
$$

#### 2. 计算 $\frac{\partial y_j}{\partial z_j}$

使用 softmax 的导数，我们可以得到：

$$
    \frac{\partial y_j}{\partial z_k} = y_j (\delta_{jk} - y_k)
$$

其中 $\delta_{jk}$ 是克罗内克（Kronecker）delta，等于 1 当 $j=k$，否则等于 0。

#### 3. 最终梯度

最终的通式为：

$$
    \frac{\partial L}{\partial z_j} = y_j - t_j
$$

这意味着，你可以直接得到关于 $z$ 的梯度：

$$
    \frac{\partial L}{\partial z} = y - t
$$

### 实现示例

以下是计算交叉熵误差及其梯度的示例代码：

```python
import numpy as np

def softmax(z):
    exp_z = np.exp(z - np.max(z))  # 为了避免溢出
    return exp_z / np.sum(exp_z, axis=1, keepdims=True)

def cross_entropy_loss(y, t):
    return -np.sum(t * np.log(y + 1e-10))  # 防止log(0)

# 示例数据
z = np.array([[1.0, 2.0, 0.5], [0.0, 1.0, 1.0]])  # 未归一化输出
t = np.array([[1, 0, 0], [0, 1, 0]])  # one-hot 编码标签

# 计算 softmax
y = softmax(z)

# 计算损失
loss = cross_entropy_loss(y, t)

# 计算梯度
grad = y - t

print("Softmax Result:\n", y)
print("Cross Entropy Loss:", loss)
print("Gradient:\n", grad)
```

### 总结

这个过程展示了如何使用交叉熵损失进行误差逆传播梯度计算。关键是利用链式法则和 softmax 函数的导数，最终得出损失相对输出和未归一化输出的梯度。这些梯度可以用于更新神经网络的权重。
